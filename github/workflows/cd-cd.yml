name : CI-CD

on:
  push:
    branches:
      - master

  jobs:
    build-and-deploy:
      runs-on : ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - uses: azure/docker-login@v1
          with:
            - login_server: testregistrysamid.azurecr.io
            - username: testregistrysamid
            - password: plKRBisvEnzLlAOcl+T+x1zz8husuMWJcVRetAYRs1+ACRAwj5XV
        - run: |
            docker build -t testregistrysamid.azurecr.io/myapp:${{ github.sha }}
            docker push testregistrysamid.azurecr.io/myapp:${{ github.sha }}
